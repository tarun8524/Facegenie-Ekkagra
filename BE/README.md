# Install Requirements.txt
```
pip install -m requirements.txt
```

# Start Running of Stream
Before change the StreamURL then run command
```
python main.py
```

# Start Running of Backend in 8000 port
```
python app.py
```